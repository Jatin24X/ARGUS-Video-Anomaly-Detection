{
  "name": "argus-stream-a-root",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix deployment/vercel_app",
    "dev": "npm run dev --prefix deployment/vercel_app"
  }
}
